# Dummy Data / Memory Store

These objects live in-memory inside `backend/src/routes/complaints.js` for now:

- **Complaints**
  - `PS-2001`: Unsafe route near metro, status `submitted`, includes timeline metadata.

Add more sample entries by editing the `complaints` array until Firestore integration is wired.
